## What and why

<!-- One paragraph. Cite the decision id this touches (D5, C18, ...); if it contradicts one, amend docs/decisions/ in this pull request. -->

## Checklist

- [ ] CI is green and the branch is rebased onto `main`.
- [ ] New source files carry the SPDX header (`AGPL-3.0-only`; `Apache-2.0` under `contracts/` and `sdk/`).
- [ ] Engine changes: I have signed the CLA (once). Contract changes: every commit is signed off (`git commit -s`).
- [ ] A user would notice this: there is a line under `Unreleased` in `CHANGELOG.md`.
- [ ] No patient data anywhere: no names, identifiers, UIDs or clinical folder paths, in code, tests, fixtures or this description.
