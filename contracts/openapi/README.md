<!-- SPDX-License-Identifier: Apache-2.0 -->

# The HTTP API contract

The one door of the engine (decision record 05 §1): resource-shaped,
versioned, job-based for anything heavy, with a bounded synchronous path
beside the jobs. `GET /api/capabilities` names the engine's version and the
contract versions it speaks.

`VERSION` is the contract version. **It changes only by a pull request that
says so**, in its title, and that bumps `VERSION` and adds a new `vN/`
directory beside the old one, which stays.

| version | document | since |
|---|---|---|
| 0 | [`v0/openapi.yaml`](v0/openapi.yaml) | Wave 4a: the document exists and is empty until `nils serve` fills it (Wave 4a §11) |
| 1 | [`v1/openapi.yaml`](v1/openapi.yaml) | Wave 4a slice 14, 2026-09-06: `nils serve`, the nineteen doors the command line has (capabilities, status, custody, audit, jobs, releases, handovers, select, review, decisions, events) |
| 2 | [`v2/openapi.yaml`](v2/openapi.yaml) | Wave 4b slice 9, 2026-09-07: version 1 plus the ask doors (schema, catalog, validate, run, jobs, explain, options, apply, diagnose, preview, describe, documents, selections, handles, values) and the session rebuild job; `capabilities.ask` carries the caps, the schema digest and the epoch |
| 3 | [`v3/openapi.yaml`](v3/openapi.yaml) | Wave 4c slice A4, 2026-09-09: version 2 plus the security scheme, the three headers (ceiling, actor, idempotency), the caller's roles and result on a queued job, the authorised and audited handle read, the capped event stream, the declaration on every answer, the guide, the draft, the diff, the value sampler, and the doors of sections 6.5 and 6.6 (packs, batches, quarantine, classify signals and try, overlays, the ingest probe), which the slices after A4 serve |
| 4 | [`v4/openapi.yaml`](v4/openapi.yaml) | 2026-09-15: version 3 with grants and detail in place of the role ladder (suite contract version 2): the caller's grants and detail in the capabilities, a policy row that names its grant, the grant or detail in place of each role note, the grant of the verb on a queued job and on a cancel, the detail every queued job records, and 403 for a caller that holds no grant |
| 5 | [`v5/openapi.yaml`](v5/openapi.yaml) | 2026-09-16: version 4 with record 26 in five slices: the dataset on a source place (what arrives, the two trees, the identity rule, what an unmapped identifier does, the cohort, the tag lists, the originals and what is held, on the sources and places doors; the dataset fields on `POST` and `PUT /api/places` under `data:work` beside `places:work`; the look at the folder on declaration and its `layout`, and the layout on the look door; `@name` as the pseudonymised tree and `@name/originals` as the originals, which a digest never reads), the pseudonymise job and its chain (`then` on `POST /api/jobs`, the verbs `pseudonymize` and `bring-in`, `then` and `chain` on a job, the batch's stages, the timeline kind `batch`, the pseudonymise step and the rates on the sources door), the identifier map and the merge (`/api/linkage/types`, `/api/linkage/imports` with its dry run, `/api/linkage/held` with `code` and `reveal` beside it, `/api/linkage/merge`, what reads an identifier under `data:work` at detail sensitive), cohorts with doors (`/api/cohorts` to list, show, make, rename, retire and change members by hand, a promotion at any grain under `data:work`, `cohort` on the review, summary and quarantine doors, `GET /api/explain/{stack}`, `datasets` and each dataset's leaving policy on a release), and every word list a site's to amend (pack contract 5: `lists` on `POST /api/classify/try` and `/api/overlays`, `by_value` on the signals, `GET /api/packs/{name}` with every axis, its values, labels, keywords, flags and thresholds and the site's terms) |
| 6 | [`v6/openapi.yaml`](v6/openapi.yaml) | 2026-09-17: version 5 plus `GET /api/pseudonymize/tags` (decision record 28), the pseudonymiser's own tag policy: the four categories it removes tag for tag and the hundred elements in them, each with its category and what becomes of it, the element the subject's code is written into, the two that are never removed and why, and the covariates a dataset keeps unless it opts out, with the switch that opts out named; numbers, categories and fates and no tag names, never the times, which are a release's, and open to any grant |
| 7 | [`v7/openapi.yaml`](v7/openapi.yaml) | 2026-09-24: version 6 with the doors of record 42. The model registry (S2): `GET` and `POST /api/models`, `GET /api/models/{id}` with its events, and `POST /api/models/{id}/admit`, `/promote` and `/retire`, under `models:see` and `models:work` (suite contract version 3); a model's answer at `POST /api/review/{id}/apply` names the registered model in `X-Nils-Actor`, is refused unless the model is admitted or promoted, is staged until a person commits it, and is answered with the model; the author of a decision is the verified actor, never the body (S1); a person's pick (`POST /api/picks`, `POST /api/picks/{id}/withdraw`, under `review:work` and for a person only), which a pick run leaves standing, and the `pick.border` review item a person's pick answers; the derivatives (`GET` and `POST /api/derivatives`, `GET /api/derivatives/{id}` and its `content`), files made from the archive kept in a working place under their sha256, the digest checked on upload, every registration and download audited, under `pipelines:see`, `pipelines:see` at detail quasi for the bytes and `pipelines:work`, with `capabilities.derivatives` saying whether a working place turns them on; campaigns (`/api/campaigns` to list and make one from a frozen selection, a handle or open review items; `/api/campaigns/{id}` with its items, assignments and agreement; its answers; claim under a lease, answer, release, an item's external metric, and the close into decisions, staged decisions, picks or nothing), label sets (`/api/label-sets` to list and to export the decisions in force on an axis, `/api/label-sets/{id}` with its labels.tsv and provenance.json, and a campaign's export), and `POST /api/decisions/commit`, the commit by filter (S5 to S7), under `campaigns:see` and `campaigns:work` of suite contract version 3, closing also under `review:work`; and, added in place by record 43 before the version's release, the pipeline catalog and runs (`GET /api/pipelines`, `GET /api/pipelines/{id}`, `GET /api/pipeline-runs`, `GET /api/pipeline-runs/{id}`, under `pipelines:see`), `run` queued at `POST /api/jobs` under `pipelines:work` at detail quasi, `capabilities.pipelines`, a run's files on the derivative listing (`run`), and the shared path of a derivative at its content door (`transport=share`); and, added in place by record 45 before the version's release, the pyramids of a selection or a handle as one job that skips what is built (`pyramid build --select | --handle` at `POST /api/jobs`) and a campaign's `pictures`, the manifest's `orientation`, `origin` and `frame`, the commit by filter's `model`, `axis`, `from`, `to` and `stacks` with a group's decision split to its part, the `axes` question held to the pack's legal combinations and closed into one decision per axis, a session item's candidates (`GET /api/campaigns/{id}/items/{item}/candidates`), a lease renewed by its holder (`POST /api/campaigns/{id}/assignments/{assignment}/renew`), answers blind to a rater until the close, an axes answer read as an object, an item that asks several axes answered whole (`values` at `POST /api/review/{id}/apply`), `from` on an `<axis>:model` group's evidence, and every door's query read decoded |

Version 0 is deliberately empty: a skeleton that a test reads, so that the
first route lands in a file that is already checked in, already versioned and
already tested, rather than in one somebody has to remember to create.
