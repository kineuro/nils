{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nils.kineuro.se/contracts/suite/v2/capabilities.schema.json",
  "title": "NILS deployment capabilities document",
  "description": "Wave 4c section 4.3: the one object the desk builds at session start and refreshes when the engine's epoch moves or a part's health changes. Nothing else in the desk may ask whether the deployment has something.",
  "type": "object",
  "required": [
    "engine",
    "person",
    "desk"
  ],
  "properties": {
    "engine": {
      "type": "object",
      "description": "GET /api/capabilities of the engine, verbatim (contracts/openapi)",
      "required": [
        "engine",
        "contracts",
        "doors",
        "policy",
        "auth",
        "principal",
        "grants",
        "detail",
        "roles",
        "registry",
        "packs"
      ],
      "properties": {
        "engine": {
          "type": "object",
          "required": [
            "name",
            "version"
          ],
          "properties": {
            "name": {
              "const": "nils"
            },
            "version": {
              "type": "string"
            }
          }
        },
        "contracts": {
          "type": "object",
          "required": [
            "openapi",
            "review_item",
            "pack"
          ],
          "properties": {
            "openapi": {
              "type": "string"
            },
            "review_item": {
              "type": "string"
            },
            "pack": {
              "type": "string"
            },
            "suite": {
              "type": "string"
            },
            "mcp": {
              "type": "string"
            }
          }
        },
        "doors": {
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^(GET|POST|PUT|DELETE) /api/"
          }
        },
        "policy": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/policy_row"
          }
        },
        "auth": {
          "type": "string",
          "enum": [
            "off",
            "token",
            "oidc"
          ]
        },
        "principal": {
          "type": "string"
        },
        "grants": {
          "type": "array",
          "items": {
            "$ref": "grants.schema.json#/$defs/grant"
          },
          "uniqueItems": true,
          "description": "What the caller holds, sorted by code point, a ceiling applied; a work grant is listed with its see"
        },
        "detail": {
          "$ref": "grants.schema.json#/$defs/detail",
          "description": "How much of a record the caller sees, a ceiling applied"
        },
        "roles": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "reader",
              "reviewer",
              "operator"
            ]
          },
          "description": "For one release: the ladder steps up to the caller's detail (plain reader; quasi reader and reviewer; sensitive reader, reviewer and operator), never admin"
        },
        "ceiling": {
          "oneOf": [
            {
              "$ref": "grants.schema.json#/$defs/step"
            },
            {
              "type": "null"
            }
          ]
        },
        "actor": {
          "$ref": "headers.schema.json#/$defs/actor"
        },
        "registry": {
          "type": "object",
          "required": [
            "epoch"
          ],
          "properties": {
            "epoch": {
              "type": "integer"
            },
            "schema_version": {
              "type": "integer"
            },
            "node": {
              "type": "string"
            }
          }
        },
        "packs": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "name",
              "version"
            ],
            "properties": {
              "name": {
                "type": "string"
              },
              "version": {
                "type": "string"
              },
              "contract": {
                "type": "integer"
              }
            }
          }
        },
        "ingest_roots": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "backup_dir": {
          "type": "boolean"
        },
        "event_streams": {
          "type": "integer"
        },
        "assist": {
          "type": [
            "string",
            "null"
          ]
        },
        "idempotency": {
          "type": "object"
        },
        "ask": {
          "type": "object"
        },
        "mcp": {
          "type": "object"
        }
      }
    },
    "kvasir": {
      "type": [
        "object",
        "null"
      ],
      "description": "GET /v1/config of Kvasir, or absent"
    },
    "assistant": {
      "type": [
        "object",
        "null"
      ],
      "description": "GET /capabilities of the assistant, or absent"
    },
    "apps": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "id"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "capabilities": {
            "type": [
              "object",
              "null"
            ],
            "description": "The app's capabilities document, or null for its absence"
          }
        }
      }
    },
    "person": {
      "type": "object",
      "required": [
        "subject",
        "display_name",
        "grants",
        "detail"
      ],
      "properties": {
        "subject": {
          "type": "string"
        },
        "display_name": {
          "type": "string"
        },
        "grants": {
          "type": "array",
          "items": {
            "$ref": "grants.schema.json#/$defs/grant"
          },
          "uniqueItems": true,
          "description": "What the person holds: the grants of every group they are in, with what was given to them alone"
        },
        "detail": {
          "$ref": "grants.schema.json#/$defs/detail",
          "description": "The highest detail of their groups and of what was given to them alone"
        },
        "groups": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "The groups the person is in, by name"
        },
        "roles": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "reader",
              "reviewer",
              "operator"
            ]
          },
          "description": "For one release: the ladder steps up to the person's detail, as the engine gives them"
        }
      }
    },
    "desk": {
      "type": "object",
      "required": [
        "version",
        "mode",
        "contracts"
      ],
      "properties": {
        "version": {
          "type": "string"
        },
        "mode": {
          "type": "string",
          "enum": [
            "off",
            "local",
            "oidc"
          ]
        },
        "contracts": {
          "type": "object",
          "description": "The contract versions the desk was generated from, by name",
          "additionalProperties": {
            "type": "string"
          }
        }
      }
    }
  },
  "$defs": {
    "policy_row": {
      "type": "object",
      "description": "One row of the engine's static policy table (Wave 4c section 6.5): what a door does, for whom, at what cost",
      "required": [
        "door",
        "grant",
        "writes",
        "idempotent",
        "cost",
        "result_cap",
        "label"
      ],
      "properties": {
        "door": {
          "type": "string"
        },
        "grant": {
          "oneOf": [
            {
              "$ref": "grants.schema.json#/$defs/grant"
            },
            {
              "type": "array",
              "items": {
                "$ref": "grants.schema.json#/$defs/grant"
              },
              "minItems": 2,
              "uniqueItems": true
            }
          ],
          "description": "The grant the door needs: one grant, or an array meaning any of them. A door open to every caller holding a grant names every grant; a door that queues a command names the grants of the verbs it queues, and each verb needs its own"
        },
        "also": {
          "$ref": "grants.schema.json#/$defs/grant",
          "description": "A second grant the door needs beside grant, as adopting an overlay needs work on review and on data"
        },
        "detail": {
          "$ref": "grants.schema.json#/$defs/detail",
          "description": "The lowest detail the door needs; absent is plain"
        },
        "writes": {
          "type": "boolean"
        },
        "idempotent": {
          "type": "boolean"
        },
        "cost": {
          "type": "string",
          "enum": [
            "free",
            "bounded",
            "job",
            "stream"
          ],
          "description": "free: one read; bounded: a read capped by the door; job: queued for a worker, answered with an id; stream: held open"
        },
        "result_cap": {
          "type": "string"
        },
        "label": {
          "type": "object",
          "required": [
            "present",
            "past"
          ],
          "properties": {
            "present": {
              "type": "string"
            },
            "past": {
              "type": "string"
            }
          }
        }
      }
    }
  }
}
